name = "echo/legacy-1-0"
version = "1.0.0"
description = "Hard-codes protocol_version veridian/1.0 in its initialize response."
protocol = "veridian/1.0"
runtime = "python"

[spawn]
command = ["${python}", "brick.py"]

[[implements]]
contract = "echo"
methods = ["say"]

[capabilities]
requires = ["contract:echo"]
