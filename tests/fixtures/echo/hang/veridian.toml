name = "echo/hang"
version = "1.0.0"
description = "Never responds to echo.say. Tests per-call timeout and hard-kill on shutdown."
protocol = "veridian/1.0"
runtime = "python"

[spawn]
command = ["${python}", "brick.py"]

[[implements]]
contract = "echo"
methods = ["say", "stream"]
