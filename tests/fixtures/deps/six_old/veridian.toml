name = "deps/six-old"
version = "0.1.0"
description = "Dependency-isolation fixture: pins six 1.15.0."
protocol = "veridian/1.0"
runtime = "python"

[spawn]
command = ["${python}", "brick.py"]

[[implements]]
contract = "tools"
methods = ["list", "invoke"]

[dependencies]
python = ["six==1.15.0"]
