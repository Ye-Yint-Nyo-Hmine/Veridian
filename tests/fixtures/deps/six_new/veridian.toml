name = "deps/six-new"
version = "0.1.0"
description = "Dependency-isolation fixture: pins six 1.16.0."
protocol = "veridian/1.0"
runtime = "python"

[spawn]
command = ["${python}", "brick.py"]

[[implements]]
contract = "workspace"
methods = ["read", "write", "list", "stat"]

[dependencies]
python = ["six==1.16.0"]
