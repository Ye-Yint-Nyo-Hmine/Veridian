"""Veridian — a plugin-native runtime for coding agents.

The kernel owns process lifecycle, the protocol, permissions, config, and events.
It owns no agent logic. Everything else is a brick.
"""

__version__ = "0.1.0"
