# example/headline-context

A custom context strategy: index only the first line / signature of each definition.

Conformance:

```bash
uv run veridian brick conformance examples/custom-context
```

See the module docstring in `brick.py` for the one-line stack binding that swaps it in.
