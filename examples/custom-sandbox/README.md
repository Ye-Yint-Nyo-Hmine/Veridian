# example/thread-sandbox

A second sandbox implementation (subprocess.run in a worker thread). Swap proof for the sandbox contract.

Conformance:

```bash
uv run veridian brick conformance examples/custom-sandbox
```

See the module docstring in `brick.py` for the one-line stack binding that swaps it in.
